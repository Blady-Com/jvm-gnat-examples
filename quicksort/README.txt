This is a demonstration of JGNAT using a quicksort program.  The
program illustrates the use of recursion and nested subprograms, as
well as simple uses of Text_IO and Integer_IO.

To execute the demo, simply type 'make run'.  Enter the amount of numbers to
be sorted when prompted by the program.
